name: Build

inputs:
  go_version:
    required: true

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Checkout iPXE
      uses: actions/checkout@v2
      with:
        path: ipxe
        repository: ipxe/ipxe
        ref: 91c77e2592f5b0e1098d3f341827d7e6925e4a85
        fetch-depth: 0 # we need the entire history to build iPXE

    - name: Setup Go
      uses: actions/setup-go@v2
      with:
        go-version: ${{ inputs.go_version }}

    - name: Go generate
      run: go generate ./...
      shell: bash
